# কিভাবে এই মসজিদ ম্যানেজমেন্ট অ্যাপটিকে অ্যান্ড্রয়েড অ্যাপে (Android APK) রূপান্তর করবেন

আমরা ইতিমধ্যে এই প্রজেক্টে **Capacitor** সেটআপ করে রেখেছি। নিচের সহজ ধাপগুলো অনুসরণ করে আপনার কম্পিউটারে একটি প্রফেশনাল অ্যান্ড্রয়েড অ্যাপ (.APK) তৈরি করে নিতে পারবেন:

## প্রয়োজনীয় সফটওয়্যার (Prerequisites)
১. আপনার পিসিতে **Node.js** ইন্সটল করা থাকতে হবে।
২. **Android Studio** ইন্সটল করা থাকতে হবে (Java SDK এবং Android SDK সহ)।

---

## ধাপ ১: প্রজেক্টটি ডাউনলোড করুন
অ্যাডমিন সেটিংস থেকে অথবা আপনার সুবিধামতো পুরো প্রজেক্ট জিপ (ZIP) আকারে ডাউনলোড করে আপনার লোকাল কম্পিউটারে এক্সট্র্যাক্ট করে নিন।

## ধাপ ২: ডিপেনডেন্সি এবং বিল্ড রানিং করা
ডাউনলোড করা ফোল্ডারের ভেতরে আপনার টার্মিনাল/কমান্ড প্রম্পট (cmd) চালু করুন এবং নিচের কমান্ডগুলো দিন:

```bash
# ডিপেনডেন্সি ইন্সটল করুন
npm install

# ওয়েব অ্যাপটি বিল্ড করুন
npm run build
```

## ধাপ ৩: অ্যান্ড্রয়েড প্ল্যাটফর্ম যুক্তকরণ (Add Android Platform)
টার্মিনালে এই কমান্ডটি দিন যাতে Capacitor-এর অ্যান্ড্রয়েড ডিরেক্টরি তৈরি হয়:

```bash
# ক্যাপাসিটার অ্যান্ড্রয়েড প্ল্যাটফর্ম যোগ করুন
npx cap add android
```

## ধাপ ৪: ওয়েব অ্যাসেট সিঙ্ক করুন (Sync Assets)
Vite বিল্ডের ফাইলগুলো অ্যান্ড্রয়েড ফোল্ডারের সাথে সিঙ্ক করুন:

```bash
npx cap sync
```

## ধাপ ৫: অ্যান্ড্রয়েড প্রজেক্ট ওপেন করা ও APK বিল্ড করা
এখন নিচের কমান্ডটির মাধ্যমে সরাসরি Android Studio-তে প্রজেক্টটি ওপেন করতে পারবেন:

```bash
npx cap open android
```

অথবা:
১. **Android Studio** চালু করুন।
২. **Open an Existing Project** ক্লিক করে আপনার প্রজেক্ট ডিরেক্টরির ভেতরের `android` ফোল্ডারটি সিলেক্ট করুন।
৩. গ্র্যাডল (Gradle) সিঙ্ক শেষ হতে দিন।
৪. মেনুবার থেকে **Build > Build Bundle(s) / APK(s) > Build APK(s)** এ ক্লিক করুন।
৫. বিল্ড সফলভাবে সম্পন্ন হলে আপনি একটি নোটিফিকেশন পাবেন, সেখানে **locate** বাটনে ক্লিক করলে আপনার কাঙ্ক্ষিত `.apk` পেয়ে যাবেন। এই APK টি আপনার ফোনে ইন্সটল করতে পারবেন!

---

## লাইভ প্রিভিউ টেস্টিং (বিকল্প মাধ্যম)
আপনি যদি ডেভলপমেন্টের সময় লাইভ চেঞ্জ দেখতে চান, তবে নিচের কমান্ডটি দিয়ে রিয়েল টাইমে আপনার ফোনে প্রিভিউ দেখতে পারবেন (আইডিয়া: পিসি এবং মোবাইল একই ওয়াইফাই নেটে থাকতে হবে):

```bash
npx cap run android --live-reload
```

---
**অভিনন্দন!** এখন আপনার পুরো মসজিদ ম্যানেজমেন্ট সিস্টেমটি মোবাইলের চমৎকার ও স্মুথ নেভিগেশন বার সম্বলিত অ্যান্ড্রয়েড অ্যাপ হিসেবে সম্পূর্ণ ব্যবহারের উপযোগী!
